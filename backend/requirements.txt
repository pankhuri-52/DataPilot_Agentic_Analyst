# DataPilot backend – FastAPI
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
python-dotenv>=1.0.0

# BigQuery (POC data & later agent queries)
google-cloud-bigquery>=3.14.0

# Gemini LLM
langchain-core>=0.3.0 # engine for our lanchain
langchain>=0.3.0 # required by langfuse LangChain CallbackHandler
langchain-google-genai>=2.0.0 # Allows langchain to talk to Gemini

# LLM observability + prompt management (Langfuse Cloud)
langfuse>=3.0.0

# SQL parsing (table allowlist / validation)
sqlparse>=0.5.0

# Agent orchestration
langgraph>=0.2.0
langgraph-checkpoint-postgres>=2.0.0
# AsyncPostgresSaver (psycopg v3); binary wheels include libpq on Windows
psycopg[binary]>=3.1.0

# Supabase (auth + chat persistence)
supabase>=2.0.0

# PostgreSQL (optional, for DATABASE_TYPE=postgres)
psycopg2-binary>=2.9.0

# Encrypted user data source configs
cryptography>=42.0.0
