# Copy this file to .env and fill in your values. Never commit .env.

# Google API key (used for Gemini LLM, and optionally BigQuery)
GOOGLE_API_KEY=AIzaSyAecDh93XiEQ1Y_1PAZ22f2nuejPkjk7ow

# BigQuery (required for POC if you use real data)
# Use the same project where you created the dataset.
BIGQUERY_PROJECT_ID=snappy-gantry-405806
BIGQUERY_DATASET=customer_data
GOOGLE_APPLICATION_CREDENTIALS=service-account.json
# Optional: path to service account JSON (or use gcloud application-default credentials)
# GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account.json
